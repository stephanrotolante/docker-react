sudo: required
services:
    - docker

before_install:
    - docker build -t stephanrotolante/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true stephanrotolante/docker-react npm run test